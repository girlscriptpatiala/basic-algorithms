//Program to check prime number
#include <stdio.h>
void main()
{
   int n=0,i=0,c=0;
   printf("Enter a number");
   scanf("%d",&n);
   for(i=1;i<=n;i++)
   {
      if(n%i==0)
      c++;
    }
    if(c==2)
    printf("%d is aprime number",n);
    else
    printf("%d is aprime number",n);
}
